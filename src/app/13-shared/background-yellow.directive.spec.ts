import { BackgroundYellowDirective } from './background-yellow.directive';

describe('BackgroundYellowDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundYellowDirective();
    expect(directive).toBeTruthy();
  });
});
